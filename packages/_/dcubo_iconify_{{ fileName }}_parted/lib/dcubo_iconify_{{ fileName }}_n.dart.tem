/// Iconify icons for {{ prefix }}.
library;

/// Class to access the icon data for {{ info.name }}.
/// All icons are stored as static const strings.
/// This class contains the icons that start with "N".
class Iconify{{ className }}IconSetN {
  {% for icon in startingNIcons %}
  /// ![preview]({{ icon.iconPreview }})
  static const {{ icon.variableName }} = '{{ icon.body }}';
  {% endfor %}
}
